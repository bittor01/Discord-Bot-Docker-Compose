services:
  hubbot:
    build: .
    container_name: hubbot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/usr/src/app/data
    # No ports exposed as this is a Discord Gateway bot
